use cream_database;
 create table cream
(name varchar(50) not null,
brand_name varchar(50) not null unique,
id int not null 
ingredients varchar(150), 
excepients varchar(150),
use varchar(150),
contra varchar(150)
);

select contra
from cream
order by brand_name;


find your way
